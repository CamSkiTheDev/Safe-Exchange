function Footer(props) {

    if (window.location.pathname === '/') return null;

    return (
        <div>
            <footer>
                <div></div>
            </footer>
        </div>
    )
}

export default Footer